<h1>Categories</h1>
  <% @categories.each do |category| %>
    <div class="panel">
      <p><%= link_to category.name, category_path(category) %></p>
    </div>
  <% end %>

<%= paginate @categories %>

<%= link_to "Add a category", new_category_path %> <br >
<%= link_to "Home", root_path %>
